[project]
name = 'dialectid'
dependencies = [
    'numpy',
    'scikit-learn>=1.3.0'
]
dynamic = ['version']

[tool.setuptools.dynamic]
version = {attr = 'dialectid.__version__'}

[tool.setuptools]
packages = ['dialectid', 'dialectid.tests']